services:
  postgres:
    image: postgres:16.4-alpine
    environment: {POSTGRES_DB: margen, POSTGRES_USER: margen, POSTGRES_PASSWORD: margen_local_only}
    ports: ['5432:5432']
  redis:
    image: redis:7.4-alpine
    ports: ['6379:6379']
